{
  "name": "backend-course-2025-5",
  "version": "1.0.0",
  "description": "Primitive caching proxy server (http.cat) — Lab 5",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js --host 127.0.0.1 --port 3000 --cache ./cache",
    "dev": "nodemon index.js -- --host 127.0.0.1 --port 3000 --cache ./cache"
  },
  "dependencies": {
    "commander": "^14.0.2",
    "superagent": "^10.2.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
